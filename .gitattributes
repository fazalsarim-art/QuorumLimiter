# Normalize line endings to LF in the repository for all text files, so shell
# scripts and Go sources are never checked out with CRLF (which would break
# bash shebangs and gofmt).
* text=auto eol=lf
*.sh text eol=lf
*.go text eol=lf
